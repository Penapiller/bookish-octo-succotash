// Trading (src/app/trades/, migrations 0015-0017) is fully built and
// tested but disabled for now while a fixed-price marketplace is built
// instead — see README "Build status". Flip this back to true to
// re-enable it later; nothing else needs to change. Every trading nav
// link, button, and page checks this flag, so flipping it is the whole
// re-enable.
export const TRADING_ENABLED = false;
